# TODO
1. The roadmap currently holds no pending tasks. Project architecture is scaled. Next steps involve identifying new product features.

## Immediate Tasks (Phase 27)
- [x] Create `Habit` model and logic in `models.py`.
- [x] Create `blueprints/habits.py` for API endpoints.
- [x] Build `HabitsTracker.vue` component and wire it to Vue store.
