# TODO
1. The roadmap currently holds no pending tasks. Project architecture is scaled. Next steps involve identifying new product features.

## Immediate Tasks (Phase 26)
- [ ] Add `delete_goal` function to `models.py`.
- [ ] Add `DELETE /api/goals/<id>` route to `blueprints/goals.py`.
- [ ] Integrate a Delete button in `ActiveGoals.vue` and `CompletedGoals.vue` within the Vue SPA.
