# TODO

## Current Tasks (Phase 34: Expanded Social Features)
- [x] Add a `likes` or `high_fives` table/column in `models.py` to allow users to react to public goals.
- [x] Implement `GET /api/social/leaderboard` in `routers/social.py` to fetch top users by streak or completion count.
- [x] Create a `Leaderboard.vue` component to display the top performers in the community.
- [x] Integrate real-time WebSocket broadcasting for global activity feeds.
- [x] Create E2E tests validating the high-five and leaderboard behaviors.

## Completed Tasks
- All Phase 1-33 tasks completed.
