# TODO
1. The roadmap currently holds no pending tasks. Project architecture is scaled. Next steps involve identifying new product features.

## Immediate Tasks (Phase 28)
- [x] Add `POST /api/goals/breakdown` to `blueprints/goals.py` simulating AI logic.
- [x] Add an `AI Spark` button to `ActiveGoals.vue` goals loop.
- [x] Implement client-side logic mapped dynamically calling AI and injecting generated sub-goals recursively correctly properly correctly seamlessly safely.
