# Deployment Instructions
Currently, the project is a local Python CLI.

## Local Setup
1. Clone the repository.
2. Ensure you have Python 3 installed.
3. Run `pip install -r requirements.txt`.
4. Run the CLI using `python cli.py`.
