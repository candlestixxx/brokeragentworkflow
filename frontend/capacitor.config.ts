import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oneminutemanager.app',
  appName: 'One-Minute Manager',
  webDir: 'dist'
};

export default config;
