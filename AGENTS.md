# Universal AI Agent Instructions
- Follow test-driven development where applicable.
- Keep components modular.
- Do not commit secrets. Use `.env` file and placeholders in `.env.example`.
- Update `CHANGELOG.md` and `VERSION.md` for user-facing changes.
- Ensure all automated checks (linting, tests) pass before proposing a commit.
